{
  "name": "googleplugin-for-aiwebassistant",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "extension"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces",
    "verify:rework": "npm run test --workspace backend && npm run test --workspace extension && npm run typecheck && npm run build"
  }
}
